# crushrc — configuration Crush pour les deux skills forensiques, avec
# Nemotron 3 Super servi en local (vLLM, NIM ou TRT-LLM, tous compatibles
# OpenAI). C'est le format COURANT de Crush : du Bash avec des builtins.
# crush.json, à côté, fait la même chose dans le format déprécié.
#
# À copier dans ~/.config/crush/crushrc (global), ou dans ./crushrc à la
# racine du dossier d'analyse (prioritaire). JAMAIS dans les scellés.
#
# Crush cherche, dans l'ordre : ./.crushrc, ./crushrc, ~/.config/crush/crushrc
# — et ~/.config suit XDG_CONFIG_HOME. Il n'exécute aucun crushrc depuis
# ~/.local/share/crush/, mais il y LIT le crush.json qu'il écrit lui-même, et
# celui-là prime sur ~/.config/crush/. Si un réglage d'ici semble sans effet,
# regardez ~/.local/share/crush/crush.json avant toute chose.
#
# L'avantage sur le JSON n'est pas cosmétique : ceci est un shell, donc $HOME
# et XDG_CONFIG_HOME y sont développés pour de vrai, partout. En crush.json,
# le ~ n'est développé QUE dans la commande d'un hook (elle passe par le shell
# embarqué) ; data_directory le prend au pied de la lettre, skills_paths le
# développe bien (manager.go:218, home.Long)
# et cherchent, ou créent, un dossier nommé « ~ ». Ici, la question ne se pose
# pas : c'est le shell qui résout, avant que Crush ne voie la valeur.
#
# ATTENTION : un crushrc est du code exécuté au lancement, dans un shell
# complet. Ne « source » jamais un fichier que vous n'avez pas lu.

CONFIG="${XDG_CONFIG_HOME:-$HOME/.config}/crush"
BASE_URL="http://dgx.local:8000/v1"      # LE serveur, nommé une fois
MODELE="dgx/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B"
# Le même modèle, sur le même serveur, déclaré une seconde fois sans
# raisonnement — voyez « le titre de session » plus bas.
MODELE_DIRECT="dgx-direct/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B"

# ── le serveur d'inférence ────────────────────────────────────────────
# Réglages tirés de la carte du modèle : temperature 1.0 et top_p 0.95 pour
# toutes les tâches, raisonnement activé par chat_template_kwargs.
# discover-models false : le poste est coupé d'Internet, le catalogue de
# modèles ne doit pas être interrogé.
provider add dgx \
    --name "Nemotron 3 Super (DGX)" \
    --type openai-compat \
    --base-url "$BASE_URL" \
    --api-key "sans-objet" \
    --discover-models false \
    --extra-body '{"chat_template_kwargs":{"enable_thinking":true}}'

# context-window : SOUS ce que sert --max-model-len, jamais égal. C'est la règle
# de ceux qui ont branché un agent sur vLLM avant nous : annoncer le maximum
# théorique fait empaqueter des invites que le serveur refuse ensuite en pleine
# session. La valeur servie se lit par
#     curl -s http://dgx.local:8000/v1/models | python3 -m json.tool
#
# La contrainte complète, S = ce que sert vLLM, M = la marge de résumé :
#     (context-window − M) + plus gros résultat d'outil + max-tokens  ≤  S
# Le plus gros résultat d'outil ici, c'est relire le rapport en cours : 20 à
# 30 k jetons. D'où, pour max-tokens 16000 et un pic de 25000 :
#     servi 131072  →  context-window 110000
#     servi 262144  →  context-window 240000   (ce qui est réglé ci-dessous)
#
# 262144 est ce que DÉCLARE le checkpoint lui-même
# (max_position_embeddings dans config.json, identique en BF16 et en FP8,
# rope_scaling null). Le million de jetons annoncé par NVIDIA est réel — RULER
# @ 1M donne 91,75 — mais il ne s'obtient pas tout seul : il faut servir avec
#     VLLM_ALLOW_LONG_MAX_MODEL_LEN=1  et  --max-model-len 1048576
# (SGLang : SGLANG_ALLOW_OVERWRITE_LONGER_CONTEXT_LEN=1 --context-length
# 1048576). Si c'est votre cas, mettez 1048576 ici aussi. Dans le doute, c'est
# la valeur SERVIE qui fait foi, et elle se lit sur le serveur :
#     curl -s http://dgx.local:8000/v1/models | python3 -m json.tool
model add "$MODELE" \
    --name "Nemotron 3 Super 120B-A12B" \
    --context-window 240000 \
    --default-max-tokens 16000 \
    --can-reason true \
    --supports-images false \
    --price-input 0 \
    --price-output 0

# Crush veut un modèle « large » et un « small » (résumés, sous-agents). Le
# DGX n'en sert qu'un : small est le même, avec un budget de réponse plus
# court. Si un second modèle est servi, c'est ici qu'il se déclare.
# max-tokens 16000 : Crush déclenche son résumé automatique quand il ne reste
# qu'une MARGE — 20000 jetons si context-window dépasse 200000, et 20 % de
# context-window sinon (internal/agent/agent.go). Le résumé arrive entre deux
# étapes : si max-tokens dépasse cette marge, la requête qui le précède dépasse
# déjà, et le serveur répond « Bad Request » en plein rapport.
#
# Rester sous la marge est nécessaire, pas suffisant : le contrôle n'a lieu
# qu'ENTRE deux étapes, et une seule étape peut ajouter un résultat d'outil
# énorme — relire un rapport de 1500 lignes, c'est 20 à 30 k jetons d'un coup.
# 16000 laisse de quoi l'absorber. C'est un choix de sécurité, pas un optimum :
# montez-le si les réponses sont tronquées, baissez-le au moindre Bad Request.
model large "$MODELE" --temperature 1.0 --top-p 0.95 --max-tokens 16000

# ── LE TITRE DE SESSION COÛTAIT AUTANT QU'UNE RÉPONSE ─────────────────
# Crush demande au modèle de nommer la session à chaque lancement. Avec
# can-reason true il alloue default-max-tokens — 16 000 jetons — au lieu
# de 40 (internal/agent/agent.go : « tok := int64(40) ; if CanReason {
# tok = DefaultMaxTokens } »), et l'extra-body force enable_thinking sur
# TOUTES les requêtes, celle-là comprise. Nemotron raisonnait donc, avec
# 16 000 jetons de budget, pour écrire cinq mots de titre. Capturé avec
# un faux vLLM : max_tokens=16000, enable_thinking=true.
#
# Second fournisseur : MÊME serveur, même URL, même modèle — sans
# raisonnement et avec un budget de 64 jetons. Le « small » y pointe, et
# c'est la seule chose qu'il sert ici : le résumé de contexte, lui, passe
# par le modèle LARGE (agent.go:1336). Capturé après : max_tokens=40,
# enable_thinking=false. L'agent ne change pas — il raisonne toujours, là
# où ça sert.
provider add dgx-direct \
    --name "Nemotron 3 Super (DGX) — sans raisonnement" \
    --type openai-compat \
    --base-url "$BASE_URL" \
    --api-key "sans-objet" \
    --discover-models false \
    --extra-body '{"chat_template_kwargs":{"enable_thinking":false}}'
model add "$MODELE_DIRECT" \
    --name "Nemotron 3 Super — titre seulement" \
    --context-window 240000 \
    --default-max-tokens 64 \
    --can-reason false \
    --supports-images false \
    --price-input 0 \
    --price-output 0
model small "$MODELE_DIRECT" --temperature 1.0 --top-p 0.95 --max-tokens 64

# ── LES OUTILS QU'ON N'A PAS ──────────────────────────────────────────
# Les définitions d'outils repartent À CHAQUE requête : 23 232 octets sur
# 47 431, soit 48 % de ce qu'on envoie. Sur un poste hors ligne, sans
# serveur de langage, sans réseau et sans MCP, dix-neuf d'entre eux ne
# peuvent rien faire. Capturé : 22 outils et 11 857 jetons avant, 9 outils
# et 9 575 jetons après — 19 % de moins sur CHAQUE appel, sans rien perdre.
# « deny » les retire de l'agent (options.disabled_tools), là où « allow »
# ne fait que sauter la demande de permission.
permissions deny fetch agentic_fetch sourcegraph download
permissions deny lsp_diagnostics lsp_references lsp_restart lsp_symbols
permissions deny lsp_definition lsp_call_hierarchy lsp_rename lsp_replace_symbol
permissions deny list_mcp_resources read_mcp_resource
permissions deny multiedit crush_logs crush_info job_output job_kill

# NB : --think et --reasoning-effort seraient INERTES ici. reasoning_effort
# exige que le modèle déclare reasoning_levels, et « think » n'est traduit que
# pour une liste d'identifiants de fournisseurs connus, dont « dgx » ne fait
# pas partie (internal/agent/coordinator.go). Le seul levier qui marche sur un
# fournisseur openai-compat maison est le --extra-body ci-dessus.

# ── les outils ────────────────────────────────────────────────────────
# Lecture seule sans confirmation : c'est tout ce dont les skills ont besoin.
# edit, write, multiedit et bash restent soumis à confirmation, exprès.
permissions allow view ls grep glob agent

# Coupés net, et cachés du modèle. Les quatre touchent au réseau : sur un
# poste d'analyse hors ligne ils ne peuvent qu'échouer bruyamment ou, pire,
# réussir. « download » écrit en plus sur le disque.
permissions deny fetch agentic_fetch sourcegraph download

# ── les chemins ───────────────────────────────────────────────────────
# skill-path n'est pas indispensable : $CONFIG/skills est déjà l'un des
# dossiers que Crush regarde d'office, avec ~/.config/agents/skills,
# ~/.agents/skills et ~/.claude/skills. Il est écrit pour que le chemin soit
# visible, et parce que c'est le seul moyen d'en poser ailleurs.
option skill-path "$CONFIG/skills"

# L'état de session de Crush. Sans ce réglage, il crée un « .crush/ » dans
# le dossier de travail — ce qui est exactement ce qu'on veut ici, un par
# affaire. Ne le posez QUE si vous voulez tout regrouper ailleurs, et
# jamais sur un chemin qui tombe dans les scellés.
# option data-directory "/chemin/absolu/vers/etat-crush"

# Poste hors ligne : ni mise à jour de catalogue, ni métriques.
option provider-auto-update false
option metrics false

# ── la ceinture : PAS ICI ─────────────────────────────────────────────
# Le hook n'est volontairement pas déclaré dans cette configuration
# globale. Il se pose dans le dossier d'analyse d'UNE machine, avec le
# script à côté — c'est le seul endroit où quelque chose se copie par
# affaire, et ça garde le Crush du poste utilisable pour tout le reste.
# Un « hooks » global s'appliquerait à tous vos autres projets.
#
# Voir README.md, « Où va quoi » : exemples/skills/crush-projet.json se
# copie en <affaire>/.crush.json, et garde-scelles.sh en
# <affaire>/outils/. En crushrc, la même chose tient en une ligne dans
# un <affaire>/.crushrc :
#
#   hook add PreToolUse --name "garde des scellés" \
#       --matcher '^(edit|write|multiedit|bash|download|lsp_rename|lsp_replace_symbol)$' \
#       --command "$CRUSH_PROJECT_DIR/outils/garde-scelles.sh" --timeout 5
